// Code generated by goa v3.0.8, DO NOT EDIT.
//
// flexapi HTTP client CLI support package
//
// Command:
// $ goa gen flexapi/design

package cli

import (
	"flag"
	flexapic "flexapi/gen/http/flexapi/client"
	"fmt"
	"net/http"
	"os"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `flexapi add
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` flexapi add --a 8334290350499145664 --b 7777782486183109549` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		flexapiFlags = flag.NewFlagSet("flexapi", flag.ContinueOnError)

		flexapiAddFlags = flag.NewFlagSet("add", flag.ExitOnError)
		flexapiAddAFlag = flexapiAddFlags.String("a", "REQUIRED", "Left operand")
		flexapiAddBFlag = flexapiAddFlags.String("b", "REQUIRED", "Right operand")
	)
	flexapiFlags.Usage = flexapiUsage
	flexapiAddFlags.Usage = flexapiAddUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "flexapi":
			svcf = flexapiFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "flexapi":
			switch epn {
			case "add":
				epf = flexapiAddFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "flexapi":
			c := flexapic.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "add":
				endpoint = c.Add()
				data, err = flexapic.BuildAddPayload(*flexapiAddAFlag, *flexapiAddBFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// flexapiUsage displays the usage of the flexapi command and its subcommands.
func flexapiUsage() {
	fmt.Fprintf(os.Stderr, `The calc service performs operations on numbers
Usage:
    %s [globalflags] flexapi COMMAND [flags]

COMMAND:
    add: Add implements add.

Additional help:
    %s flexapi COMMAND --help
`, os.Args[0], os.Args[0])
}
func flexapiAddUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] flexapi add -a INT -b INT

Add implements add.
    -a INT: Left operand
    -b INT: Right operand

Example:
    `+os.Args[0]+` flexapi add --a 8334290350499145664 --b 7777782486183109549
`, os.Args[0])
}
